import { FlContentDirective } from './content.directive';

describe('FlContentDirective', () => {
  it('should create an instance', () => {
    const directive = new FlContentDirective();
    expect(directive).toBeTruthy();
  });
});
