import { SubheaderComponent } from './subheader.component';

describe('SubheaderDirective', () => {
  it('should create an instance', () => {
    const directive = new SubheaderComponent();
    expect(directive).toBeTruthy();
  });
});
